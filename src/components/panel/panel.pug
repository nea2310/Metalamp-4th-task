include ../input-field/input-field.pug
include ../toggle/toggle.pug
include ../radiobuttons/radiobuttons.pug
include ../button/button.pug

mixin panel({radioGroupName = ''} = {})
  -
    const elementName = 'panel'

  div(class = `${elementName} js-${elementName}`)
    fieldset(class = `${elementName}__main-setup`)
      legend Main
      div(class = `${elementName}__main-setup-component js-${elementName}__min`)
        +input-field({
          labelText: 'min',
          usage: 'min'})
      div(class = `${elementName}__main-setup-component js-${elementName}__max`)
        +input-field({
          labelText: 'max',
          usage: 'max'})
      div(class = `${elementName}__main-setup-component js-${elementName}__from`)
        +input-field({
          labelText: 'from',
          usage: 'from'})
      div(class = `${elementName}__main-setup-component js-${elementName}__to`)
        +input-field({
          labelText: 'to',
          usage: 'to'})
      div(class = `${elementName}__main-setup-component js-${elementName}__vertical`)
        +toggle({
          name: 'vertical',
          usage: 'vertical'})
      div(class = `${elementName}__main-setup-component js-${elementName}__range`)
        +toggle({
          name: 'range',
          usage: 'range'})
      div(class = `${elementName}__main-setup-component js-${elementName}__bar`)
        +toggle({
          name: 'bar',
          usage: 'bar'})
      div(class = `${elementName}__main-setup-component js-${elementName}__tip`)
        +toggle({
          name: 'tip',
          usage: 'tip'})
    fieldset(class = `${elementName}__scaleSetup`)
      legend Scale
      div(class = `${elementName}__scaleSetup-component ${elementName}__scaleSetup-component_type_scale js-${elementName}__scale`)
        +toggle({
          name: 'scale',
          usage: 'scale'})
      span scale base
      div(class = `${elementName}__scaleSetup-component ${elementName}__scaleSetup-component_type_scaleBase js-${elementName}__scaleBase`)
        +radiobuttons({
          categories:
          [{name:'step', usage: 'step', radioGroupName: ''}, 
          {name:'interval', usage: 'interval', radioGroupName: ''}],
          radioGroupName})
      div(class = `${elementName}__scaleSetup-component ${elementName}__scaleSetup-component_type_IntervalStep`)
        div(class = `${elementName}__scaleSetup-component ${elementName}__scaleSetup-component_type_step js-${elementName}__step`)
          +input-field({
            labelText: 'step',
            usage: 'step'})
        div(class = `${elementName}__scaleSetup-component  ${elementName}__scaleSetup-component_type_interval js-${elementName}__interval`)
          +input-field({
            labelText: 'interval',
            usage: 'interval'})
    fieldset(class = `${elementName}__moveSetup`)
      legend Control movement
      div(class = `${elementName}__moveSetup-component js-${elementName}__sticky`)
        +toggle({name: 'sticky',
          usage: 'sticky'})
      div(class = `${elementName}__moveSetup-component js-${elementName}__shiftOnKeyDown`)
        +input-field({
          labelText: 'shift on key down',
          usage: 'shiftOnKeyDown'})
      div(class = `${elementName}__moveSetup-component js-${elementName}__shiftOnKeyHold`)
        +input-field({
          labelText: 'shift on key hold',
          usage: 'shiftOnKeyHold'})
    fieldset(class = `${elementName}__actions`)
      legend Actions
      div(class = `${elementName}__actions-component`)
        +toggle({
          name: 'subscribe events',
          usage: 'subscribe'})
      div(class = `${elementName}__actions-component`)
        +toggle({
          name: 'disable',
          usage: 'disable'})
      div(class = `${elementName}__actions-component`)
        +toggle({
          name: 'destroy',
          usage: 'destroy'})
    div(class = `${elementName}__apply`)
      +button({text: 'Apply changes'})



